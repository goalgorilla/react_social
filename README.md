# React Application with Open Social API on Platform.sh

It is setup as a multi-application project where:

- the backend `api` application is running on `PHP:`
- the frontend `app` application is running on `nodejs:10`
