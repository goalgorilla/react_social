import styled from "styled-components";

const RecentActivityTitle = styled.p`
  font-size: ${props => props.theme.font.size.medium};
  font-weight: ${props => props.theme.font.weight.bold};
`;

export default RecentActivityTitle;
